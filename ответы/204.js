// Объявите функцию foo со входящим параметром a. 
// Внутри неё объявите функцию bar со входящим параметром b. 
// Обозначьте границы параметров функции foo. 
// Обозначьте границы параметров функции bar.

function foo(a) {/* начало ф-и foo */
  function bar(b) {/* тело ф-и */}
/* конец ф-и foo */}
